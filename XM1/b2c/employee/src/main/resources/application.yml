spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:b2cmall.db
  redis:
    host: 47.100.22.158
    port: 6379
    database: 0
    timeout: 5000
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.zsl.b2cmall.employee.entity
  configuration:
    map-underscore-to-camel-case: true
